#include <iostream> 
#include <string> 
using namespace std; 
int main() 
{ 
  int a,b; 
  cout<<"Enter the number of year";
  cin >> a; 
  if (a%100 == 0) 
    b=a/100; 
  else 
    b=a/100 +1; 
  cout << b <<"century";
}
