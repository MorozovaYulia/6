#include <iostream> 
#include <string> 
using namespace std; 
int main() 
{ 
  int a,b,c, S, s, n; 
  cout<<"Enter the numbers ";
  cin >> a >> b >> c; 
  S=a*b; 
  s=c*c; 
  n=S/s; 
  S-= s*n; 
  cout << S << "\n" << n; 
}
